# from os import environ

# def __init__():
#     db_user = environ.get("DB_USER", None)
#     db_password = environ.get("DB_PASSWORD", None)
#     if "DB_PASSWORD_FILE" in environ:
#         with open(environ["DB_PASSWORD_FILE"]) as reader:
#             db_password = reader.read()
#     if not username and not password:
#         raise RuntimeError("database credentials not configured in environment")

